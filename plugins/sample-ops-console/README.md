# Sample Ops Console Plugin

This sample plugin exists to validate end-to-end plugin discovery for both:

- backend manifest loading
- frontend module discovery

It is intentionally minimal and should stay aligned with the current kernel contracts.
