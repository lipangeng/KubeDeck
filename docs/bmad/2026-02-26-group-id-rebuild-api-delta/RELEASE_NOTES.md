# Release Notes Delta

- Added controlled rebuild API: `POST /api/iam/groups/rebuild-ids`.
- Added API-level conflict mapping for canonical duplicate names.
- Added endpoint regression tests and retained full backend green status.
