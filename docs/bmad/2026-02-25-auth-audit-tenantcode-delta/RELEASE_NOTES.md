# Release Notes Delta

- Added strict tenant_code format validation for auth entry points.
- Added login audit event lifecycle (attempted/failed/succeeded).
- Added backend regression tests for validation and audit events.
