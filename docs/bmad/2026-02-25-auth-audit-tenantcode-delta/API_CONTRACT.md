# API Contract Delta

## POST /api/auth/login
- New validation error: `invalid_tenant_code` (400).

## POST /api/auth/switch-tenant
- New validation error: `invalid_tenant_code` (400).

## POST /api/auth/oauth/callback
- New validation error: `invalid_tenant_code` (400).
