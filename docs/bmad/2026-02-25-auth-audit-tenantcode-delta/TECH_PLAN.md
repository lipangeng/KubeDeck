# Tech Plan Delta

- Add `normalizeTenantCode` helper in auth handler.
- Reuse validation across login/switch/oauth callback.
- Replace login audit action with attempted/failed/succeeded event model.
- Add regression tests in `meta_handler_test.go`.
